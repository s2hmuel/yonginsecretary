spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/project
    username: player
    password: 1234

    devtools:
      livereload:
        enabled: true
      restart:
        enabled: true
    thymeleaf:
      cache: false


  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true


  servlet:
    multipart:
      maxFileSize: 100MB
      maxRequestSize: 100MB

